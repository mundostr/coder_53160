/**
 * Esquema base para el entregable1 (challenge1)
 * 
 * Clase ProductManager con métodos para agregar y retornar productos.
 * Ver slides por detalles del entregable:
 * - propiedades del producto
 * - identificador, código, etc.
 */

class ProductManager {
    constructor() {
        // Declarar un array vacío para productos
    }

    /**
     * Debe recibir un objeto con los campos indicados
     * y realizar un push al array de productos
     */
    addProduct(product) {}

    /**
     * Debe retornar el array de productos
     */
    getProducts() {
    }

    /**
     * Debe recibir como parámetro un id, buscar en el array de
     * productos y retornarlo. En caso de no encontrarlo, debe
     * retornar el mensaje Not found.
     */
    getProductById(id) {}
}
